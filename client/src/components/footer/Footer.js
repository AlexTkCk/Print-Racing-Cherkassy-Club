import React from 'react';
import './Footer.css'

const Footer = ({text}) => {
    return (
        <footer className={'footer'}>
            All rights reserved
        </footer>
    );
};

export default Footer;